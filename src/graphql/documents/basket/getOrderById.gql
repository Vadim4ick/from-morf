query GetOrderById($id: ID!) {
  orders_by_id(id: $id) {
    id
    totalPrice
    created_at
    items {
      ...OrderItemFragment
    }

    user {
      first_name
      last_name
      id
    }
  }
}
